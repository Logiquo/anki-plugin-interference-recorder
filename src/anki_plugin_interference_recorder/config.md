# Interference Recorder configuration

- `shortcut`: the Reviewer shortcut used to record an interference. The default is `0`.
- `decay`: the decay factor used when interference links are evaluated. The default is `0.9`.

Interference is always tracked at card granularity.
